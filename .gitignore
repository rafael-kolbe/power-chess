node_modules/

# Local browser cache directory (can remain from previous UI tooling)
.pw-browsers/

# Match debug file logs (ADMIN_DEBUG_MATCH)
logs/

# Local secrets (compose / env)
.env
.env.local
.env.*.local
test-result
test-result/
test-results/
*.out
